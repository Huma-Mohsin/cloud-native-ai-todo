# Phase III AI-Powered Chatbot - Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: File name must be .env.local for Next.js (not .env)!

# ============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================================================
# Neon Serverless PostgreSQL connection string
# MUST be the SAME database as backend
# Format: postgresql://user:password@host:port/database?ssl=require
# Get this from: https://console.neon.tech/
DATABASE_URL=postgresql://your-user:your-password@your-host.neon.tech/neondb?ssl=require

# ============================================================================
# AUTHENTICATION (REQUIRED)
# ============================================================================
# Better Auth secret key (MUST be 32+ characters)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# MUST match the value in backend/.env
BETTER_AUTH_SECRET=your-32-character-secret-key-here-change-this

# ============================================================================
# APPLICATION URLS
# ============================================================================
# Frontend URL (for redirects and OAuth callbacks)
# Local: http://localhost:3002
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Backend API URL
# Local: http://localhost:8000
# Production: https://your-api-domain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# GOOGLE OAUTH (OPTIONAL)
# ============================================================================
# Google OAuth credentials from Google Cloud Console
# Get from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {NEXT_PUBLIC_APP_URL}/api/auth/callback/google
# Example: http://localhost:3002/api/auth/callback/google

# Google Client ID
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google Client Secret
GOOGLE_CLIENT_SECRET=your-client-secret

# ============================================================================
# NOTES
# ============================================================================
# 1. DATABASE_URL: MUST be the same database used by backend
# 2. BETTER_AUTH_SECRET: MUST be identical to backend .env
# 3. NEXT_PUBLIC_APP_URL: Must match the port your frontend runs on (default: 3002)
# 4. Google OAuth is optional - you can use email/password only
# 5. For Google OAuth, configure redirect URI in Google Cloud Console
# 6. Never commit .env.local file to version control (it's in .gitignore)
# 7. File name must be .env.local for Next.js (not .env)
